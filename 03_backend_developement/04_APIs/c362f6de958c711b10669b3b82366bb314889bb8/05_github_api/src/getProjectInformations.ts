import * as request from "request"

function getProjectInformations(url, callback): void {
  // code the function here
}

// Leave the line below for tests and `src/index.ts` to work properly
export { getProjectInformations }
