import { Db } from "mongodb";

type Aggregation = Record<string, unknown>[];

type Results = Record<string, unknown>[];

type SearchQuery = {
  [key: string]: unknown;
};

export function search() {
  /**
   * Complete this function with:
   * - typed parameters
   * - return type
   * - the function code 😁
   */
}
